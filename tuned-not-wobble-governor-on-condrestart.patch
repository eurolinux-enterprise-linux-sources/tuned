diff --git a/ktune/ktune.init b/ktune/ktune.init
index 55495a6..808f371 100644
--- a/ktune/ktune.init
+++ b/ktune/ktune.init
@@ -25,6 +25,7 @@ SAVE_FILE="/var/run/ktune.save"
 SYSCTL_SET="/sbin/sysctl -q -w "
 SYSCTL_GET="/sbin/sysctl -e"
 
+RESTART=
 KERNEL_ELEVATOR="cfq"
 /bin/fgrep -q 'elevator=' /proc/cmdline
 CMDLINE_ELEVATOR=$?
@@ -115,11 +116,11 @@ load_file() {
 
 call_script() {
     local script=$1
-    local option=$2
+    shift
 
     if [ -x "$script" ]; then
-	echo -n "Calling '$script $option': "
-	("$script" "$option")
+	echo -n "Calling '$script $@': "
+	("$script" "$@")
 	ret=$?
 
 	if [ $ret -eq 0 ]; then
@@ -173,7 +174,7 @@ revert_sysctl() {
 
     if [ ${#KTUNE_FILES[@]} -gt 0 ]; then
 	for file in "${KTUNE_FILES[@]}"; do
-	    call_script "${file%.conf}.sh" stop
+	    call_script "${file%.conf}.sh" stop ${RESTART:+restart}
 	done
     fi
 }
@@ -287,6 +288,7 @@ case "$1" in
 	;;
     condrestart|try-restart)
 	[ -f "$VAR_SUBSYS_KTUNE" ] || exit 0
+	RESTART=1
 	stop
 	start
 	RETVAL=$?
diff --git a/tune-profiles/enterprise-storage/ktune.sh b/tune-profiles/enterprise-storage/ktune.sh
index 2046ece..23b5747 100755
--- a/tune-profiles/enterprise-storage/ktune.sh
+++ b/tune-profiles/enterprise-storage/ktune.sh
@@ -12,7 +12,7 @@ start() {
 }
 
 stop() {
-	restore_cpu_governor
+	[ "$1" != "restart" ] && restore_cpu_governor
 	restore_transparent_hugepages
 	enable_disk_barriers
 	restore_disk_readahead
diff --git a/tune-profiles/functions b/tune-profiles/functions
index 864209e..a2b5000 100644
--- a/tune-profiles/functions
+++ b/tune-profiles/functions
@@ -135,6 +135,8 @@ CPUSPEED_ORIG_GOV="/var/run/tuned/ktune-cpuspeed-governor-%s.save"
 CPUSPEED_STARTED="/var/run/tuned/ktune-cpuspeed-started"
 CPUSPEED_CFG="/etc/sysconfig/cpuspeed"
 CPUSPEED_INIT="/etc/init.d/cpuspeed"
+# do not use cpuspeed
+CPUSPEED_USE="0"
 CPUS="$(ls -d1 /sys/devices/system/cpu/cpu* | sed 's;^.*/;;' |  grep "cpu[0-9]\+")"
 
 # set CPU governor setting and store the old settings
@@ -142,24 +144,32 @@ CPUS="$(ls -d1 /sys/devices/system/cpu/cpu* | sed 's;^.*/;;' |  grep "cpu[0-9]\+
 set_cpu_governor() {
 	governor=$1
 
-	# prefer governor setting using cpuspeed daemon
+	# always patch cpuspeed configuration if exists, if it doesn't exist and is enabled,
+	# explictly disable it with hint
 	if [ -e $CPUSPEED_INIT ]; then
 		if [ ! -e $CPUSPEED_SAVE_FILE -a -e $CPUSPEED_CFG ]; then
 			cp -p $CPUSPEED_CFG $CPUSPEED_SAVE_FILE
 			sed -e 's/^GOVERNOR=.*/GOVERNOR='$governor'/g' $CPUSPEED_SAVE_FILE > $CPUSPEED_CFG
 		fi
+	else
+		if [ "$CPUSPEED_USE" = "1" ]; then
+			echo >&2
+			echo "Suggestion: install 'cpuspeed' package to get best tuning results." >&2
+			echo "Falling back to sysfs control." >&2
+			echo >&2
+		fi
+
+		CPUSPEED_USE="0"
+	fi
 
+	if [ "$CPUSPEED_USE" = "1" ]; then
 		service cpuspeed status &> /dev/null
 		[ $? -eq 3 ] && touch $CPUSPEED_STARTED || rm -f $CPUSPEED_STARTED
 
 		service cpuspeed restart &> /dev/null
 
-	# direct change using /sys fs
+	# direct change using sysfs
 	elif [ -e /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor ]; then
-		echo >&2
-		echo "Suggestion: install 'cpuspeed' package to get best tuning results." >&2
-		echo "Falling back to '$governor' scaling governor for all CPUs." >&2
-		echo >&2
 
 		for cpu in $CPUS; do
 			gov_file=/sys/devices/system/cpu/$cpu/cpufreq/scaling_governor
@@ -182,13 +192,21 @@ restore_cpu_governor() {
 			rm -f $CPUSPEED_SAVE_FILE
 		fi
 
+		if [ "$CPUSPEED_USE" = "1" ]; then
+			if [ -e $CPUSPEED_STARTED ]; then
+				service cpuspeed stop &> /dev/null
+			else
+				service cpuspeed restart &> /dev/null
+			fi
+		fi
 		if [ -e $CPUSPEED_STARTED ]; then
 			rm -f $CPUSPEED_STARTED
-			service cpuspeed stop &> /dev/null
-		else
-			service cpuspeed restart &> /dev/null
 		fi
-	elif [ -e /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor ]; then
+	else
+		CPUSPEED_USE="0"
+	fi
+
+	if [ "$CPUSPEED_USE" != "1" -a -e /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor ]; then
 		for cpu in $CPUS; do
 			cpufreq_dir=/sys/devices/system/cpu/$cpu/cpufreq
 			save_file=$(printf $CPUSPEED_ORIG_GOV $cpu)
@@ -423,8 +441,10 @@ status() {
 
 process() {
 	VAR_SUBSYS_KTUNE="/var/lock/subsys/ktune"
+	ARG="$1"
+	shift
 
-	case "$1" in
+	case "$ARG" in
 	start)
 		[ -f "$VAR_SUBSYS_KTUNE" ] && exit 0
 		start
@@ -432,7 +452,7 @@ process() {
 		;;
 	stop)
 		[ -f "$VAR_SUBSYS_KTUNE" ] || exit 0
-		stop
+		stop "$@"
 		RETVAL=$?
 		;;
 	reload)
diff --git a/tune-profiles/laptop-battery-powersave/ktune.sh b/tune-profiles/laptop-battery-powersave/ktune.sh
index c3b83ea..d79b397 100755
--- a/tune-profiles/laptop-battery-powersave/ktune.sh
+++ b/tune-profiles/laptop-battery-powersave/ktune.sh
@@ -19,7 +19,7 @@ stop() {
 	set_disk_alpm max_performance
 	#disable_usb_autosuspend
 	disable_cpu_multicore_powersave
-	restore_cpu_governor
+	[ "$1" != "restart" ] && restore_cpu_governor
 	enable_snd_ac97_powersave
 	enable_cd_polling
 	disable_wifi_powersave
diff --git a/tune-profiles/latency-performance/ktune.sh b/tune-profiles/latency-performance/ktune.sh
index b9ddf47..d143df3 100755
--- a/tune-profiles/latency-performance/ktune.sh
+++ b/tune-profiles/latency-performance/ktune.sh
@@ -11,7 +11,7 @@ start() {
 }
 
 stop() {
-	restore_cpu_governor
+	[ "$1" != "restart" ] && restore_cpu_governor
 	/usr/libexec/tuned/pmqos-static.py disable
 	restore_transparent_hugepages
 	return 0
diff --git a/tune-profiles/oracle/ktune.sh b/tune-profiles/oracle/ktune.sh
index d85475d..90f26a3 100755
--- a/tune-profiles/oracle/ktune.sh
+++ b/tune-profiles/oracle/ktune.sh
@@ -13,7 +13,7 @@ start() {
 }
 
 stop() {
-	restore_cpu_governor
+	[ "$1" != "restart" ] && restore_cpu_governor
 	restore_transparent_hugepages
 
 	return 0
diff --git a/tune-profiles/sap-hana-vmware/ktune.sh b/tune-profiles/sap-hana-vmware/ktune.sh
index 1a987a9..16a3db2 100755
--- a/tune-profiles/sap-hana-vmware/ktune.sh
+++ b/tune-profiles/sap-hana-vmware/ktune.sh
@@ -12,7 +12,7 @@ start() {
 }
 
 stop() {
-	restore_cpu_governor
+	[ "$1" != "restart" ] && restore_cpu_governor
 	/usr/libexec/tuned/pmqos-static.py disable
 	restore_transparent_hugepages
 	restore_disk_readahead
diff --git a/tune-profiles/sap-hana/ktune.sh b/tune-profiles/sap-hana/ktune.sh
index 59a11c4..b938435 100755
--- a/tune-profiles/sap-hana/ktune.sh
+++ b/tune-profiles/sap-hana/ktune.sh
@@ -15,7 +15,7 @@ start() {
 }
 
 stop() {
-	restore_cpu_governor
+	[ "$1" != "restart" ] && restore_cpu_governor
 	/usr/libexec/tuned/pmqos-static.py disable
 	restore_transparent_hugepages
 	restore_disk_readahead
diff --git a/tune-profiles/sap-netweaver/ktune.sh b/tune-profiles/sap-netweaver/ktune.sh
index 8e84bfe..a79795c 100755
--- a/tune-profiles/sap-netweaver/ktune.sh
+++ b/tune-profiles/sap-netweaver/ktune.sh
@@ -15,7 +15,7 @@ start() {
 }
 
 stop() {
-	restore_cpu_governor
+	[ "$1" != "restart" ] && restore_cpu_governor
 	restore_transparent_hugepages
 	enable_disk_barriers
 	restore_disk_readahead
diff --git a/tune-profiles/throughput-performance/ktune.sh b/tune-profiles/throughput-performance/ktune.sh
index 671f4fe..8225ee4 100755
--- a/tune-profiles/throughput-performance/ktune.sh
+++ b/tune-profiles/throughput-performance/ktune.sh
@@ -10,7 +10,7 @@ start() {
 }
 
 stop() {
-	restore_cpu_governor
+	[ "$1" != "restart" ] && restore_cpu_governor
 	restore_transparent_hugepages
 
 	return 0
diff --git a/tune-profiles/virtual-guest/ktune.sh b/tune-profiles/virtual-guest/ktune.sh
index d06776f..c0016a3 100755
--- a/tune-profiles/virtual-guest/ktune.sh
+++ b/tune-profiles/virtual-guest/ktune.sh
@@ -11,7 +11,7 @@ start() {
 }
 
 stop() {
-	restore_cpu_governor
+	[ "$1" != "restart" ] && restore_cpu_governor
 	restore_transparent_hugepages
 	restore_disk_readahead
 
diff --git a/tune-profiles/virtual-host/ktune.sh b/tune-profiles/virtual-host/ktune.sh
index 2046ece..23b5747 100755
--- a/tune-profiles/virtual-host/ktune.sh
+++ b/tune-profiles/virtual-host/ktune.sh
@@ -12,7 +12,7 @@ start() {
 }
 
 stop() {
-	restore_cpu_governor
+	[ "$1" != "restart" ] && restore_cpu_governor
 	restore_transparent_hugepages
 	enable_disk_barriers
 	restore_disk_readahead
